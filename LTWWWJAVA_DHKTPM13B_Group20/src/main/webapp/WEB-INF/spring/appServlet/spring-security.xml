<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:sec="http://www.springframework.org/schema/security"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans  
	    http://www.springframework.org/schema/beans/spring-beans.xsd  
	    http://www.springframework.org/schema/context 
		https://www.springframework.org/schema/context/spring-context.xsd
	    http://www.springframework.org/schema/security  
	    http://www.springframework.org/schema/security/spring-security.xsd">
	<debug />



	<http auto-config="true">
		<csrf disabled="true" />
		<intercept-url pattern="/" access="permitAll()" />
		<intercept-url pattern="/welcome" access="permitAll()" />
		<intercept-url pattern="/login" access="permitAll()" />
		<intercept-url pattern="/logout" access="permitAll()" />

		<intercept-url pattern="/userInfo"
			access="hasAnyRole('ROLE_USER', 'ROLE_ADMIN')" />
		<intercept-url pattern="/admin"
			access="hasRole('ROLE_ADMIN')" />

		<form-login login-page="/sign-in"
			login-processing-url="/j_spring_security_check"
			always-use-default-target="false" default-target-url="/index"
			authentication-failure-url="/sign-in?error=false"
			username-parameter="email" password-parameter="password" />

		<logout logout-url="/sign-out" logout-success-url="/index"
			delete-cookies="JSESSIONID" />
	</http>



<!-- 	authentication from database -->
	<authentication-manager>

		<authentication-provider
			user-service-ref="customUserDetailsService">
			<password-encoder ref="passwordEncoder" />
		</authentication-provider>

	</authentication-manager>

<!-- 	userDetails -->
	<beans:bean id="accountPricipal"
		class="fit.se.main.principal.AccountPricipal"></beans:bean>

<!-- 	Password Encoder -->
	<beans:bean id="passwordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
		<beans:constructor-arg value="11" />
	</beans:bean>

<!-- 	userDetailsService -->
	<beans:bean id="customUserDetailsService"
		class="fit.se.main.principal.CustomUserDetailsService"></beans:bean>


</beans:beans>